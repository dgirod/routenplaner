name: routenplaner
version: '3.8'
services: {app: {build: ., ports: ['3000:3000'], environment: ['NODE_ENV=production'], env_file: ['.env'], restart: always}, webhook: {build: {context: ., dockerfile: Dockerfile.webhook}, ports: ['9000:9000'], env_file: ['.env'], volumes: ['/var/run/docker.sock:/var/run/docker.sock', '/root/routenplaner:/repo'], restart: always}}
